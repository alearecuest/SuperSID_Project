import React from 'react';
import { Link } from '@chakra-ui/react';

const Navbar = () => {
    return (
        <nav>
            <Link href="/">Home</Link>
        </nav>
    );
};

export default Navbar;